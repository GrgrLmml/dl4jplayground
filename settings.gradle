pluginManagement {
    repositories {
        maven {
            url publicUrl
            credentials {
                username mavenUser
                password mavenPassword
            }
        }
        gradlePluginPortal() // Include public Gradle plugin portal
    }
}